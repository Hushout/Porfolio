import { types } from "./types";

// export const setLoginErrorMessage = loginErrorMessage => async dispatch => await dispatch({ type: types.setLoginErrorMessage, loginErrorMessage });

// export const setRegisterErrorMessage = registerErrorMessage => async dispatch => await dispatch({ type: types.setRegisterErrorMessage, registerErrorMessage });